if(COMP_DOCS)
install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/
        DESTINATION  ${AFNI_INSTALL_DOC_DIR}
        FILE_PERMISSIONS OWNER_READ GROUP_READ WORLD_READ
        FILES_MATCHING PATTERN "README*"
        )
endif()