PROJECT(NIFTICDFLIB)

INCLUDE_DIRECTORIES(${NIFTILIB_SOURCE_DIR})
#INCLUDE_DIRECTORIES(/usr/include/nifti)
SET(NIFTICDFLIB_SRC nifticdf.c)

SET(LIB_NAME ${PACKAGE_PREFIX}nifticdf) 

ADD_LIBRARY(${LIB_NAME} ${NIFTICDFLIB_SRC} )

INSTALL_TARGETS(${LIB_INSTALL_DIR} ${LIB_NAME})
INSTALL_FILES(${INCLUDE_INSTALL_DIR} "(\\.h)$")
