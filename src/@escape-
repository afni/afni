#/bin/tcsh -f
#Add a shell escape before the first dash in argument
#  @escape- one -two three
set  ll = ()
foreach jjj ($*)
   set a = `echo $jjj | sed 's/^-/\\-/'` 
   set ll = ($ll "$a")
end
echo "$ll"
