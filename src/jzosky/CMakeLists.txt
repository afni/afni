PROJECT(JZOSKY)

# script being programs on their own
SET(SCRIPTS RetroTS.py)

INSTALL(PROGRAMS ${SCRIPTS}
  DESTINATION ${AFNI_INSTALL_SCRIPT_DIR} COMPONENT Runtime)

INSTALL(DIRECTORY lib_RetroTS
  DESTINATION ${AFNI_INSTALL_SCRIPT_DIR} COMPONENT Runtime)
