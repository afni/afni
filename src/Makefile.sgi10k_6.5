## for SGI (IRIX 6.5, R10000 chip)
## (change gcc <-> cc to use GCC or SGI compiler, as case may be)

# USE_ZLIB = -DHAVE_ZLIB
# LZLIB    = -lz

CCDEBS = -DAFNI_DEBUG -DIMSEQ_DEBUG -DDISPLAY_DEBUG -DTHD_DEBUG
CC     = cc -w -O2 -DSGI -DUSE_TRACING -DHAVE_XDBE
CCVOL  = cc -w -O2 -DSGI -DUSE_TRACING -DHAVE_XDBE
CCFAST = $(CC)
CCMIN  = cc
CCD    = $(CC) $(CCDEBS)
CCOLD  = $(CC)

IFLAGS = -I. -I/usr/Motif-2.1/include
LFLAGS = -s -L. -L/usr/Motif-2.1/lib -Wl,-x

CCSVD  = cc -O0


PLUGIN_SUFFIX = so
PLUGIN_LFLAGS = -shared -x
PLUGIN_CC     = $(CC)
PLFLAGS       = -L. -Wl,-x

AR     = /usr/bin/ar
RANLIB = /sbin/touch
TAR    = /bin/tar
MKDIR  = /bin/mkdir
LD     = /bin/ld
GZIP   = /usr/local/bin/gzip

RM = /bin/rm -f
MV = /bin/mv -f
CP = /bin/cp -f

LINT = /usr/bin/lint -a -b -u -v -x $(IFLAGS) $(CCDEFS)

INSTALLDIR = ./sgi10k_6.5
LIBDIR = $(INSTALLDIR)
SHOWOFF = -DSHOWOFF=sgi10k_6.5

LLIBS  = -lmri -lXm -lXt -lXext -lX11 $(LZLIB) -lm -lc


###############################################################

MAKE = make
include Makefile.INCLUDE
